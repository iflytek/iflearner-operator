apiVersion: git.iflytek.com/v1
kind: IflearnerJob
metadata:
  name: iflearnerjob-client2
spec:
  role: client
  host: job1.b.party.iflearner.com
  template:
    spec:
      restartPolicy: Never
      containers:
      - image: iflearner:0.1.3   
        name: iflearnerjob-client
        imagePullPolicy: IfNotPresent
        workingDir: /iflearner/examples/homo/quickstart_tensorflow
        args:      
        - sleep
        - 1d

        # - python
        # - -u
        # - quickstart_tensorflow.py
        # - --name=test
        # - --epochs=10
        # - --server=s1.server.iflearner.com:30031
        # - --cert=/etc/server-iflearner-secret.crt
